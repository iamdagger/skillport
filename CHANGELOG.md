# Changelog

## 0.6.2
- Improve error messages when sync marker is missing.
- Pin skillport-presets to v0.2.1.

## 0.6.1
- Cache the installed pack under `~/.skillport` to skip re-sync on startup.

## 0.6.0
- Add `skillport sync`.
- Extract preset definitions into `skillport-presets` package.

## 0.5.0
- Initial public release.
